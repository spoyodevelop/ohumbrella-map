# 리팩터링 체크리스트

작은 책임 분리부터 진행하고, 외부 계약이나 DB를 바꾸는 작업은 별도 단계로 다룬다.
각 단계는 `npm run check`와 `npm run build`가 통과하는 크기로 커밋한다.

## 완료한 기반 작업

- [x] 클라이언트의 HTTP 요청을 `fetchJson`으로 통일한다.
- [x] 날씨 데이터 로딩을 `useWeatherData`로 분리한다.
- [x] React Compiler를 Vite 빌드에 연결한다.
- [x] 클라이언트의 넓은 범위에서 사용되는 상태 변수 이름을 정리한다.
- [x] 서버를 포함한 TypeScript 검사 명령을 구성한다.
- [x] 지도 hover 상태와 사전 로딩을 제거하고 SVG 기본 툴팁을 사용한다.
- [x] 드래그·휠·핀치 중 지도 transform을 React 렌더와 분리한다.

## 0. 현재 변경 정리

- [x] `server/api.ts`의 설명을 되풀이하던 주석 제거분을 검토한다.
- [x] 체크리스트 변경과 주석 정리를 별도 커밋으로 나눈다.

## 1. 서버 쿼리 책임 분리

완료했다. 동작, SQL, 반환 형태와 공개 함수 이름은 그대로 유지했다.

- [x] `server/queries.ts`의 타입과 공용 보조 함수를 분리한다.
- [x] 변경 전 `npm run check`와 `npm run build`를 통과하는 기준점을 남긴다.
- [x] 관측·예보 저장 및 갱신 쿼리를 쓰기 모듈로 분리한다.
- [x] 현재 날씨·시계열·예보 조회 쿼리를 조회 모듈로 분리한다.
- [x] 확률·신뢰도·정확도 쿼리를 분석 모듈로 분리한다.
- [x] `server/api.ts`와 `server/weather/kma.ts`의 import를 새 경계에 맞춘다.
- [x] 순환 의존성과 불필요한 재노출이 없는지 확인한다.
- [x] 분리 전후의 API 응답 형태가 같은지 확인한다.

## 2. 기상청 수집 모듈 책임 분리

`server/weather/kma.ts`의 현재 동작을 먼저 보존한 뒤 파일 경계만 정리한다.

- [ ] 요청·재시도 로직과 수집 흐름을 분리한다.
- [ ] 기상청 발표 시각 계산을 별도 모듈로 분리한다.
- [ ] 원본 응답 파싱과 내부 저장 레코드 변환을 분리한다.
- [ ] 실황 수집과 예보 수집의 공통 흐름이 실제로 같은 경우에만 공통화한다.
- [ ] 제공 시각, 재시도 정책 등 외부 제약을 설명하는 주석은 유지한다.

## 3. 서버 진입점과 라우트 정리

- [ ] `server/index.ts`, `server/api.ts`, `server/worker.ts`의 시작 책임을 구분한다.
- [ ] 환경 변수 로딩이 여러 진입점에 중복된 이유를 확인하고 한곳으로 모을지 결정한다.
- [ ] 수동 동기화와 워커가 각각 가진 `isSyncing`의 범위가 의도한 것인지 확인한다.
- [ ] 반복되는 API 오류 응답을 작은 공용 처리로 묶을지 검토한다.
- [ ] `catch (err: any)`를 `unknown` 기반 처리로 바꾼다.
- [ ] 라우트 파일은 실제로 더 커질 때 날씨·분석·외부 프록시 단위로 나눈다.

## 4. 날씨 데이터 계약 정리

파일 분리가 끝난 뒤 진행한다. 이 단계부터는 클라이언트와 서버를 함께 변경한다.

- [ ] 클라이언트와 서버가 공유하는 날씨 API 계약을 만든다.
- [ ] 서버의 저장 레코드, 쿼리 결과, API 응답 타입을 구분한다.
- [ ] 기상청 원본 `PTY`와 내부 `precipitationType`을 분리한다.
- [ ] 기존 DB의 `pty` 컬럼과 내부 타입 사이에 명시적인 변환 경계를 둔다.
- [ ] 중복된 `pop`과 `kmaPop`의 역할을 정리한다.
- [ ] 숫자로 전달되는 `isRaining`을 boolean으로 바꿀지 결정한다.
- [ ] `pop`, `rn1`, `tmp`, `sky`는 외부·DB 경계에서만 유지하고 내부 도메인 이름을 정한다.
- [ ] 클라이언트의 도달 불가능한 PTY 분기를 제거하거나 새 내부 타입에 맞춘다.

## 5. DB 스키마와 마이그레이션

API 계약 정리와 분리해서 진행한다. 기존 데이터 보존 방법을 정하기 전에는 컬럼을 바꾸지 않는다.

- [ ] 현재 스키마 생성과 마이그레이션의 책임이 섞여 있는지 점검한다.
- [ ] `hourly_weather`와 관측·예보 테이블의 중복 책임을 문서화한다.
- [ ] 컬럼 이름 변경이 필요한지 확정한다.
- [ ] 변경한다면 기존 로컬 SQLite와 Turso 모두를 위한 마이그레이션 절차를 만든다.
- [ ] 뷰와 backfill이 재실행되어도 안전한지 확인한다.

## 6. 검증 기반 보강

- [x] 쿼리 모듈 분리 전에 현재 주요 API 응답의 최소 회귀 기준을 정한다.
- [ ] 기상청 시각 계산과 PTY 변환처럼 순수한 로직부터 단위 테스트를 추가한다.
- [ ] DB 쿼리는 임시 SQLite를 사용하는 통합 테스트가 필요한지 결정한다.
- [ ] 모든 작업 단위에서 `npm run check`와 `npm run build`를 실행한다.
- [ ] React Compiler가 실제 빌드 결과에 적용되는지 설정 변경 시 다시 확인한다.

## 이름과 주석 원칙

- 공개 경계와 넓은 범위의 변수는 역할이 드러나는 이름을 사용한다.
- 짧은 지역 범위의 관용적인 이름은 억지로 늘리지 않는다.
- 코드 내용을 되풀이하는 주석은 이름이나 구조를 개선한 뒤 제거한다.
- 외부 API 규칙, 정책, 선택 이유를 설명하는 주석은 유지한다.
- 이름 변경만으로 여러 파일을 흔들 경우 얻는 이점이 분명한지 먼저 확인한다.

## 현재 범위에서 하지 않는 것

- 자동 polling은 도입하지 않는다. 사용자가 필요할 때 새로고침한다.
- 모든 API 응답에 Zod 같은 런타임 검증을 일괄 도입하지 않는다.
- `fetchJson<T>`의 타입 단언은 신뢰하는 내부 API 경계에서 유지한다.
- DB 컬럼 변경과 대규모 이름 변경을 작은 모듈 분리 작업에 섞지 않는다.
- 단순히 파일 줄 수를 줄이기 위한 추상화는 만들지 않는다.
